services:
  sv3-player-site:
    build: .
    container_name: sv3-player-site
    restart: unless-stopped
    ports:
      - "127.0.0.1:4444:3000"
    networks:
      - containers-network
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  containers-network:
    external: true
